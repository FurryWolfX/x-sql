<root namespace="test">
    <query name="getUser">
        select * from user where 1=1
        <if condition="@flag === 1">
            and name = @name
        </if>
        and id in (
        <for seperator="," array="idList">
            @id
        </for>
        )
    </query>
</root>
