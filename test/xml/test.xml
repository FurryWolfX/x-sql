<root namespace="test">
    <query name="getUser">
        SELECT * FROM user WHERE 1=1
        <if condition="@flag === 1">
            AND name = @name
            AND id IN (
            <for separator="," array="idList">
                @id
            </for>
            )
        </if>
    </query>
</root>
